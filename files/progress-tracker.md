# Svasthi — Progress Tracker

Update this yourself as you go. Status options: `Not started` · `In
progress` · `Built (unverified)` · `Verified` · `Blocked`.

"Verified" means you personally did the "you verify" checklist for that
ticket in `build-plan.md` — not just that the AI tool said it was done.

| # | Ticket | Status | Verified on | Notes |
|---|---|---|---|---|
| 1 | F01 — Persistence, auth, journey state | Built (unverified) | | 2026-09-21: Code complete. Migration applied to live project (verified: 5 tables, RLS enabled, 13 policies). RLS isolation programmatically verified — cross-user reads/deletes blocked, forged user_id inserts rejected 403, anon sees nothing (see Known Issues for method). Still needs: Ravi's own click-through (sign-up/login/logout in the app) per the ticket's "you verify" step. |
| 2 | F11 (start) — Test harness | Built (unverified) | | 2026-09-21: Vitest 3.2.7 (pinned — v5 conflicts with @types/node 20 used by Next 14), minimal config with @ alias, smoke test, `npm run test`. Real output shown: 1 test passed; tsc/lint/build also pass. Ravi verifies via verification-guide.md Category 2. |
| 3 | F00 — Product shell & design system | Built (unverified) | | 2026-09-21: Tokens wired in tailwind.config.ts (sage palette added — earlier components referenced sage classes that didn't exist). Built: PageHeader, ProgressBar, EmptyState, ErrorState, SupportBanner (persistent, solid bg, tel:14416 user-initiated), MobileNav bottom bar, NavLinkAction; PrimaryButton gained href-link twin + secondary variant; AppShell rewritten — header nav with 4 primary destinations + active state, skip link, SupportBanner on every page, MobileNav, Link-based routing. Home page + placeholders rebuilt on shared components. Unit tests for nav model. Still needs Ravi's click-through per the ticket's "you verify" step (360px no-overflow, keyboard nav, reduced motion). |
| 4 | F02 — Daily check-in | Built (unverified) | | 2026-09-21: /check-in real page — MoodCard (native radio group), stress/energy sliders (announce "Not set yet" until touched — no hidden 0 default), sleep hours input, optional context checkboxes (max 8, matches checkInSchema). Pure validation in src/lib/check-in/domain.ts (unit-tested: 8 tests). Duplicate-submit guarded while busy; on failure values preserved + retry message; success updates journey state via addCheckIn and shows today's summary. Re-check-in allowed and both entries kept. Still needs Ravi's click-through: submit and confirm the row in Supabase under own user id. |
| 5 | F08 (banner) — Always-visible support | Not started | | |
| 6 | F03 — Voice journal recording | Built (unverified) | | 2026-09-22: Real recorder at /journal — MediaRecorder via src/lib/audio/use-recorder.ts (permission only after click, 60s cap with timer + hard backstop, real elapsed timer + AnalyserNode level meter, tracks stopped & object URLs revoked on every exit path incl. cancel/unmount/permission-pending). Typed reflection is first-class (TranscriptEditor) — works with mic denied, no device, or unsupported browser; recorder error copy points to it. Audio never uploaded: measured locally (src/lib/audio/analyze.ts → features.ts, unit-tested: silence/NaN/cap/word-rate), measurements omitted honestly if the clip can't be decoded. Journal submission is a review gate: page owns the recorder controller; "Use this recording" keeps the CURRENT take only (re-recording invalidates). Per-entry consent: two explicit save buttons (allow / no AI processing). Crisis panel on crisisSignal from POST /api/journals. GET /api/journals added (limit 90) and journals now load into JourneyProvider state. Contract change (features optional) documented in docs/api-contract.md. Unit tests: audio-features (7) + journal-domain (6), all passing; lint/tsc/build verified. Still needs Ravi's click-through per the ticket's "you verify" step: record/stop/play on a real phone + one desktop browser, and the denied-mic typed path. |
| 7 | F04 — Descriptive voice signals | Built (unverified) | | 2026-09-22: Math layer existed from F03 (src/lib/audio/features.ts, 10 unit tests). Added VoiceSignals.tsx — measured cards (duration, avg loudness dBFS, quiet-ratio %, words/min) each with plain-language explanation + Measured badge; wording is session-specific ("this recording"), never a personal baseline. Missing rmsDb renders "not captured" (honest omission); undecodable-clip state explains values are left out, not estimated; typed reflections show no signal section at all. Wired into /journal's saved view (only when the saved entry actually has features). Honesty fix: calculateLevel no longer uses acoustics at all — pauses alone previously escalated to "watch"/referral, the exact defect architecture.md's risk table assigns to F04; fallback evidence wording updated to "Measured from your audio…" and pinned by 6 new unit tests (fallback-insight.test.ts). lint/tsc/build/34 tests pass. Still needs Ravi's click-through: loud vs quiet vs silence recordings show sensibly different numbers, and no wording reads as diagnosis. |
| 8 | F08 (finish) — Full crisis support & safety | Built (unverified) | | 2026-09-22: Detector expanded from 3 to 19 patterns — direct, indirect/veiled, third-person/quoted, and Hinglish distress phrasing (marna chahta/chahti hun, jeene ka man nahi, khudkhushi, jaan dena); apostrophes handled for straight + typographic forms. Recall-biased policy documented in crisis.ts header: negation/quotes NOT filtered (over-trigger is the safe direction); one precision carve-out — bare "is killing me" colloquialism does not trigger, reflexive forms always do. tests/unit/crisis.test.ts (10 tests) pins trigger examples AND a precision floor on ordinary journal text. New reusable CrisisPanel (immediate-danger vs counselling split, tel:14416 user-initiated, limitation copy inline) now used by /journal crisis state and /support page (rebuilt around it). Safety checks confirmed present on all three content paths: journals (crisisSignal), chat (crisis routing before model call + storage), insights (crisis before Gemini). No Niramaya branding remains (code_search verified). lint/tsc/build/44 tests pass. Pending: qualified human review (gate) — pattern list exported for exactly this. |
| 9 | F05 — Personal insights | Built (unverified) | | 2026-09-22: /insights page — consent gate (declined → nothing sent, choice respected with link back to consent control), signed-out gate, honest pending copy (no fake progress), duplicate-submit guard, disabled generate until a check-in AND a reflection exist (with links to whichever is missing). InsightCard: AI badge with source named ("AI-generated wording" vs "Guided response (no AI used)"), level chip (Steady/Worth watching/Support suggested), EvidenceList under a Self-reported badge, suggestion block, onward links to /exercises + /support, fixed disclaimer. Model output now strictly Zod-validated (insightOutputSchema: title ≤200, evidence ≤3×300, suggestion ≤1000) BEFORE the UI; safety fields (level/referral/disclaimer/crisis) are application-owned and never accepted from the model — pinned by 6 new unit tests (incl. injection-shaped extra fields stripped). 8s deadline enforced twice: SDK httpOptions + an explicit Promise.race (AI_CONFIG.timeoutMs). VERBATIM prompt from dawn-and-insight-prompts.md now lives in src/lib/ai/prompts.ts with the FIXED_NON_MEDICAL_DISCLAIMER constant shared by gemini.ts and fallback.ts. /exercises placeholder added so the insight card's action link works before F09. lint/tsc/build/50 tests pass. Still needs Ravi's read-through of generated insights per the ticket's "you verify" step. |
| 10 | F06 — Dawn AI companion | Built (unverified) | | 2026-09-23: /dawn real chat page — sign-in gate (no anonymous chat, per F01 decision) and consent gate (declined → nothing sent, respected with onward links to consent + /support). Server: POST /api/chat now uses the VERBATIM Dawn system prompt from dawn-and-insight-prompts.md (shared DAWN_SYSTEM_PROMPT in prompts.ts); crisis check still runs before any model call and before storage; bounded history unchanged at max 6 prior messages. NEW GET /api/chat returns own last 20 rows for refresh-restore (contract documented). Fallback upgraded from a single generic line to reviewed contextual variants (overwhelm / low-mood / sleep / work / gratitude / general) with a pure, unit-tested selector (classifyDawnContext + selectDawnReply, 13 tests incl. honesty pins: no diagnosis wording, ≤1 question, hints only to /exercises or /support); test also pins that the verbatim prompt keeps its safety lines. MessageBubble always shows the AI badge and names the source ("AI-generated wording" vs "Guided response (no AI used)"). ChatPanel: suggested opening prompts fill the draft (never auto-send), duplicate-submit guarded, honest clear-conversation scope (client-side only — copy states server retention is not claimed erased), retry-safe error handling (user's turn stays, "Your message was not sent"), aria-live message region. Crisis signal from the API raises the shared CrisisPanel above the chat. lint/tsc/63 tests/build pass. Still needs Ravi's click-through: real conversation incl. a distress phrase → panel appears, and refresh restores history. |
| 11 | F07 — Wellness dashboard | Not started | | |
| 12 | F09 — Guided exercise | Not started | | |
| 13 | F10 — Resource directory | Not started | | |
| 14 | F11 (finish) — Full tests, CI, deploy | Not started | | |
| — | **Gate:** Human security review of RLS policies | Not started | | Required before real users |
| — | **Gate:** Qualified human review of crisis detection & copy | Not started | | Required before real users |

## Known issues log

Use this to track things you notice that aren't fixed yet — a running list
so nothing gets silently forgotten between sessions with your AI tool.

| Found on | Issue | Fixed? |
|---|---|---|
| F01 build | Migration `20260921000000_f01_schema_rls.sql` was written but NOT yet applied to the live Supabase project (verified via REST: PGRST205 "table not found"). App returns 503/401 at runtime until it is run in the Supabase SQL Editor. | **Fixed 2026-09-21** — applied via Management API; tables/policies confirmed live. RLS isolation test (Category 3): two throwaway accounts, user2 saw 0 of user1's rows, anon saw 0, cross-user delete = 0 rows, forged user_id insert → 403. Test users deleted; auth email-confirm setting restored to its original value. |
| F01 build | Crisis copy in `src/lib/safety/crisis.ts` said "Niramaya" — replaced with "Svasthi" during F01. Qualified human review still pending (gate). | Copy replaced; review pending |
| F00 build | AuthPanel/ConsentPanel used `sage-*` Tailwind classes with no sage palette defined in tailwind.config.ts (silently unstyled borders/badges). | **Fixed 2026-09-21** — sage 50–900 added to the config |
| F02 build | Sliders show a thumb at 0 before the user touches them (visual only); the form treats untouched sliders as "not set" and requires explicit values, matching the no-hidden-defaults rule. | Accepted trade-off |
| F03 build | If the browser can't decode a kept recording, the entry saves with NO measurements (honest omission) rather than invented numbers — the insights fallback correspondingly cites acoustics only when features exist. | By design (data honesty) |
| F03 build | Mic behaviour (recording, playback, denial, cleanup) is not machine-testable here; verified only by code review + unit tests of the pure math. Ravi's real-device click-through is the actual check. | Pending Ravi |
| F04 build | Insight level/referral no longer reacts to voice measurements at all (was: pauseRatio ≥ 0.35 alone → "watch"). Gemini prompt still receives features as data with an explicit "never claim voice metrics establish a condition" instruction; output stays schema-checked. | Fixed 2026-09-22 (was the F04-defect from architecture.md) |
| F04 build | The measured dBFS/quiet-ratio numbers are not machine-testable against real microphones; unit tests cover the pure math and honesty properties only. | Pending Ravi (loud/quiet/silence click-through) |
| F08 build | Detector recall-biased: negated/quoted/third-person phrases trigger a support panel on purpose ("I would never kill myself" → panel). Copy written non-alarmist; accepted as the safe direction. | Accepted trade-off |
| F08 build | Bare colloquial "is killing me" (movies, workouts) deliberately does NOT trigger; reflexive forms ("kill myself") always do. Chosen so everyday journals don't train users to ignore the panel. | Accepted trade-off |
| F08 build | Devanagari-script and non-Hinglish Indian-language distress phrases are NOT detected — documented limitation, stated in /support copy. Broadening needs the qualified human review anyway. | Documented limitation |
| F05 build | Insight generation requires both a check-in and a reflection (server contract refines both to be present); the button stays disabled with a link to whichever is missing rather than erroring after the fact. | By design (contract shape) |
| F05 build | Model wording is validated but not deep-semantics-checked — the prompt forbids diagnoses and phone numbers, and output is schema-checked, but wording review is Ravi's read-through + the human gate. | Pending Ravi + gate |
| Pre-existing | Streak & wellness-score formula kept (now labeled illustrative and derived from real dates); full provenance-badged dashboard is F07. | Planned F07 |

## Decisions made along the way

A short log of any real decisions that diverge from the plan in the other
context files, so future-you (or a future AI session) knows why.

| Date | Decision | Reason |
|---|---|---|
| 2026-09-21 | Chat now requires sign-in and stores both sides of the conversation in `chat_messages` (was anonymous/ephemeral); bounded to 6 prior messages as history | F01: no shared session; per-user durable storage; prepares F06 bounded history |
| 2026-09-21 | `/api/dashboard` returns only real user rows (seeded sample days removed from the server); sample fixtures moved client-side to `src/lib/demo/fixtures.ts` for F07 to badge | Data-honesty rule: server never blends real and fictional data |
| 2026-09-21 | `/api/journals` response now includes `crisisSignal: boolean` | Lets the client surface support immediately; contract change documented in docs/api-contract.md |
| 2026-09-21 | Nav destinations exist as honest "not built yet" placeholder pages; `/support` is a real page | Support access must never be a dead link; nav must not 404 |
| 2026-09-21 | Primary nav reduced to the four ui-rules.md destinations (Home, Journal, Dawn, Trends) in a header with active states; /check-in moved to contextual CTAs on Home; "Get support" lives in SupportBanner + MobileNav + footer, not the nav row | ui-rules.md navigation table; support is persistent everywhere but deliberately not a competing nav item |
| 2026-09-22 | journalSchema `features` made optional; typed journals get no voice_features row | F03 acceptance: typed path must work fully without a microphone; inventing zeros would violate the data-honesty rules |
| 2026-09-22 | Journal audio is measured locally with the browser's AudioContext and never uploaded; features omitted when measurement fails | No audio-upload endpoint exists in the core build; honest omission beats fabricated readings |
| 2026-09-22 | Journal page collects consent per entry (two save buttons) instead of relying only on the home-page global toggle | F03 requires consent before processing; the decision must sit at the point of submission, visible with the words it applies to |
| 2026-09-22 | Added GET /api/journals (own rows, newest first, limit 90) and journals/set reducer action | /journal shows recent reflections after a refresh; mirrors the GET /api/check-ins pattern |
| 2026-09-22 | calculateLevel dropped acoustics entirely; voice signals are display-only | architecture.md F04: pauses reflect noise, recording quality, and speech differences — they must never drive a clinical/referral classification |
| 2026-09-22 | Crisis detector optimized for recall over precision; negation/quote filtering deliberately omitted | A false positive shows a calm panel; a false negative hides support from someone who needs it. Panel copy stays gentle so an unnecessary trigger is harmless |
| 2026-09-22 | One precision carve-out: bare "killing me" colloquialism doesn't trigger, reflexive forms do | Constant false positives would train users to dismiss the one panel that matters |
| 2026-09-22 | Insight safety fields (level, referral, disclaimer, crisis) are computed in app logic and never accepted from model output; the model supplies wording only | dawn-and-insight-prompts.md: "the application supplies safety routing, referral flags, provenance, and the fixed nonmedical disclaimer" — a model can't be trusted to self-limit |
